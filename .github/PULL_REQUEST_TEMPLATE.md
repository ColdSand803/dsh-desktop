<!-- Keep this short. Delete anything that does not apply. -->

## What this changes

## Why

<!-- If it changes a behaviour that has a comment explaining why it was done that
     way, please update that reasoning too. -->

## How it was verified

- [ ] `cargo fmt --check`
- [ ] `cargo clippy --all-targets -- -D warnings`
- [ ] `cargo test`
- [ ] Ran it in a window with a real `dsh` on PATH

<!-- The last box is genuinely optional -- much of this app cannot be checked
     without a GUI. Saying "compiles and tests pass, not run in a window" is more
     useful than leaving it ambiguous. Two bugs in this repo's history survived
     because "it compiles" was treated as "it works". -->

## User-visible behaviour

<!-- Does this add or change anything the user sees or that runs on their machine?
     If it adds something that happens automatically, say how they turn it off. -->
